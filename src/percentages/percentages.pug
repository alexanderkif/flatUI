include ../header/header
include ../percentage/percentage
include ../sectors/sectors

mixin percentages({header,values,sectors})
    .percentages
        .percentages__header 
            +header(header)
        .percentages__content
            each value in values
                +percentage(value)
            +sectors(sectors)