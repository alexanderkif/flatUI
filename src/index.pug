
include /flatUI/flatUI

- var dataJson = require('./data.json');

html
    head
        meta(charset='UTF-8')
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title flatUI
        link(href="img/favicon.ico" rel="icon" type="image/x-icon")
        
    body
        +flatUI(dataJson)
        