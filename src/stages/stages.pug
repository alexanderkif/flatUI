include ../components/header/header
include ../components/stage/stage

mixin stages({header,steps,passed})
    .stages
        header.stages__header 
            +header(header)
        .stages__content
            +stage(steps,passed)